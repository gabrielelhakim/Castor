pipeline {
  agent any
  stages{
    stage('stage1'){
      steps{
        echo "Hello ia m tony tannous"
        bat "mvn clean install"
    } 
    }
  }
  /* stages {
    stage('Unit Test q') { 
      node {
  bat "mvn clean install"
}
    }
    stage('Deploy Standalone') { 
      node {
  bat "mvn clean install"
}
    }
    stage('Deploy Cloudhub DEV') { 
      node {
  bat "mvn clean install"
}
    }
  } */
}
